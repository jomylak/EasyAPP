# Workday employer registry for ApplyPilot
# Users can add their own employers by following this pattern.
# Find your employer's Workday URL at: https://[company].wd[N].myworkdayjobs.com
#
# Emptied out: discovery for this profile is restricted to intern-list.com /
# newgrad-jobs.com (see config/sites.yaml). The full default list (48 Workday
# employers) is still in git history — `git show HEAD:src/applypilot/config/employers.yaml`
# from the ApplyPilot repo, or `git checkout -- src/applypilot/config/employers.yaml`,
# restores it.

employers: {}

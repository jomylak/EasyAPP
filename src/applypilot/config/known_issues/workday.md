- Workday ATS is down for maintenance — the URL redirects to https://community.workday.com/maintenance-page with "Workday is currently unavailable. We are experiencing a service interruption." Cannot access job posting or application form.
- Tesla.com returns 403 Access Denied (Akamai CDN blocking automated browsers) on all pages including the job posting URL. Tesla's ATS (Workday at tesla.wd1.myworkdayjobs.com) is under scheduled maintenance — all requests redirect to community.workday.com/maintenance-page, and the CXS API returns HTTP 422 errors. No alternative application pathway (Greenhouse, Lever, email) exists for this posting. Multiple search engines were unable to locate a direct Workday apply link that bypasses these blocks.
